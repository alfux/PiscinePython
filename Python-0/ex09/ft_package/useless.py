def count_in_list(lst: list, wht: any) -> int:
    """Counts number of items in a list"""
    return (lst.count(wht))


def print_legend() -> None:
    """Yes, like in the movie"""
    print("I am legend.")


def double_sandwich(x: any) -> any:
    """Double it, double it, double it again"""
    return (x + x)
